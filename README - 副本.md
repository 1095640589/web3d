## git 

```sh
// 初始化
git init

// 下载项目 XXX为项目地址
git clone XXX

// 添加某文件
git add XXX
//添加目录下所有文件
git add . 

//创建仓库的整个流程 命令
git init

git clone XXX / 
git remote add origin XXX 、
git pull origin master

git add .
git commit -m "XXX"
git push origin master

```

